services:
  web:
    image: odoo:17.0
    restart: always
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8070:8069"
      - "8072:8072"
    volumes:
      - odoo-web-data:/var/lib/odoo
      - ./files/config:/etc/odoo
      - ./files/addons:/mnt/extra-addons
    entrypoint: "/usr/bin/odoo -c /etc/odoo/odoo.conf"
    networks:
      - odoonet
    labels:
      # Habilitar Traefik para este servicio
      - "traefik.enable=true"
      # Aplicar middleware CORS desde file provider
      - "traefik.http.middlewares.odoo-cors-custom.headers.accesscontrolalloworiginlist=*"
      - "traefik.http.middlewares.odoo-cors-custom.headers.accesscontrolallowmethods=GET,POST,PUT,DELETE,OPTIONS"
      - "traefik.http.middlewares.odoo-cors-custom.headers.accesscontrolallowheaders=content-type,x-api-key,authorization,accept"
      - "traefik.http.middlewares.odoo-cors-custom.headers.accesscontrolmaxage=86400"

  db:
    image: postgres:15
    restart: always
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=odoo
      - POSTGRES_PASSWORD=admin
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U odoo -d postgres" ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    volumes:
      - odoo-db-data:/var/lib/postgresql/data
    networks:
      - odoonet

volumes:
  odoo-web-data:
  odoo-db-data:


networks:
  odoonet:
